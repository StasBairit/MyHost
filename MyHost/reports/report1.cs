using System;
using System.Collections.Generic;
using System.Text;
//using Microsoft.Reporting.WinForms;

namespace MyHost.reports
{
    class report1
    {
        private string name = "report1";
        public report1()
        { }
        
        public void print()
        {
            /*LocalReport report = reportController.getLocalReport(name);

            report.DataSources.Add(new ReportDataSource(
                "ReportDataSet_OrderCompilationTable", getOrderCompilationData(order)));
            report.DataSources.Add(new ReportDataSource(
                "ReportDataSet_OrderCompilationInfo", getOrderCompilationInfo(order)));
            reportController.print(report);*/
            
        }
    }
}
